specVersion: "v1beta"
serviceId: "projectfish"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "projectfish-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
